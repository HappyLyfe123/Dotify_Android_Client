<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">#242424</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/search_icon</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@android:color/white</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">Test</item>

        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="NoSaveEditTExt" parent="Base.Widget.AppCompat.AutoCompleteTextView">
        <item name="android:saveEnabled">false</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/app_logo</item>
    </style>
</resources>
